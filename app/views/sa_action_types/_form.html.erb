<%= form_for(sa_action_type) do |f| %>
  <% if sa_action_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sa_action_type.errors.count, "error") %> prohibited this sa_action_type from being saved:</h2>

      <ul>
      <% sa_action_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">

    <%= f.label :name %><br>
    <%= f.text_field :name,  class: "form-control" %>

  </div>


  <div class="actions">
    <%= f.submit :class => 'btn btn-primary'  %>
  </div>
<% end %>
