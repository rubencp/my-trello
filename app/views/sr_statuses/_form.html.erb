<%= form_for(sr_status) do |f| %>
  <% if sr_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sr_status.errors.count, "error") %> prohibited this sr_status from being saved:</h2>

      <ul>
      <% sr_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">

    <%= f.label :name %><br>
    <%= f.text_field :name %>

  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
