<%= form_for(document_status) do |f| %>
  <% if document_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document_status.errors.count, "error") %> prohibited this document_status from being saved:</h2>

      <ul>
      <% document_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">

    <%= f.label :name %><br>
    <%= f.text_field :name,  class: "form-control" %>

  </div>


  <div class="actions">
    <%= f.submit :class => 'btn btn-primary'  %>
  </div>
<% end %>
