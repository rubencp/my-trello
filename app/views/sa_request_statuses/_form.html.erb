<%= form_for(sa_request_status) do |f| %>
  <% if sa_request_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sa_request_status.errors.count, "error") %> prohibited this sa_request_status from being saved:</h2>

      <ul>
      <% sa_request_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">

    <%= f.label :name %><br>
    <%= f.text_field :name,  class: "form-control" %>

  </div>


  <div class="actions">
    <%= f.submit :class => 'btn btn-primary'  %>
  </div>
<% end %>
